import { McpServer } from '@modelcontextprotocol/sdk/server/mcp.js';

type Configuration = {
    apiKey?: string;
    app?: string;
    agent?: boolean;
    actions?: string[];
};

declare class MatonAgentToolkit extends McpServer {
    private _maton;
    constructor(configuration: Configuration);
}

export { MatonAgentToolkit };
